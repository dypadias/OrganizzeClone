<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matos.diego.organizze">

    <application
        android:allowBackup="true"
        android:icon="@drawable/quatro"
        android:label="@string/app_name"
        android:roundIcon="@drawable/um"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.DespesasActivity"
            android:theme="@style/despesasTheme"
            android:exported="false" />
        <activity
            android:name=".activity.ReceitasActivity"
            android:theme="@style/receitasTheme"
            android:exported="false" />
        <activity
            android:name=".activity.PrincipalActivity"
            android:exported="false"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.CadastroActivity"
            android:exported="false" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="false" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:theme="@style/Theme.Intro" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>